plugins {
    id 'java-library'
}

dependencies {
    implementation 'org.jetbrains:annotations:26.1.0'
    api 'com.google.code.gson:gson:2.14.0'
}

tasks.named('jar', Jar) {
    archiveFileName = 'atools.baseparty.jar'
}
